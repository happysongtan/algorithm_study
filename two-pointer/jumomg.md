##풀이 문제
- 주소

##풀이 아이디어
-입력된 값을 배열로 저장 및 정렬해준다.
-두 숫자의 인덱스를 start 와 end로 구분하고 end를 배열의 size-1 로 선언해준다
-if문을 통하여 두 숫자가 target보다 작으면 start++
-target보다 크면 end--
-target과 같으면 count++, start++,end--를 실행해준다

##시간 복잡도
-O(N log N)

##느낀점
-투포인터의 시작점을 0 0 에서 시작했는데, 시작점을 변경하는게 더 관리하기 수월했다.

