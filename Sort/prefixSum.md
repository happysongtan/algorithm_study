##풀이 문제
- 주소

##풀이 아이디어
-입력받은 배열을 int배열로 바꿔주고 각 배열을 더하는 sum배열도 추가해준다.
-구간의 합은 start와 end로 나눠주고 특정 구간의 합이 sum_arr[end-1]-sum_arr[start-2]임으로 계산하여 답을 구해준다.

##시간 복잡도
-O(N)

##느낀점
-배열을 선언할때 구간의 합을 계산하는 sum을 통해서 효율적으로 관리할 수 있었다.
