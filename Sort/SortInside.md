## 풀이 문제
- 주소

## 풀이 아이디어
- 선택 정렬을 2중for문으로 구현하여 정렬함
- sort로 구현시 Arrays.sort(arr,Collection.reverseOrder()) 를 사용하여 역순으로 정렬한다.
## 시간 복잡도
- O(N^2)

## 느낀점
- 선택 정렬을 함수를 사용하지 않고 2중for문을 사용하여 구현하였다.
- Collections.reverseOrder()를 사용하려면 char[]가 아닌 Character[] 배열을 사용해야 한다.

