import java.util.*;
class Solution {
    public void diff(){
        for(int i=0;i<;i++){
            if(visited[i]==false){
                visited[i]==true;
                dfs(current+number[i]);
                visited[i]=false;
            }
        }
    }
    public int solution(String numbers) {
        boolean visited=new boolean<>();
        int answer = 0;
        return answer;
    }
}
